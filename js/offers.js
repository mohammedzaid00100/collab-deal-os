(function(){
  document.addEventListener('DOMContentLoaded',async()=>{if(document.body.dataset.page!=='offers')return;const d=await CDOS.loadData();const box=document.getElementById('offersList');if(!box)return;box.innerHTML=d.offers.map(o=>`<div class="card dash-card row-between"><div><div class="pill">${o.status}</div><h3 style="margin:10px 0 4px">${o.brand}</h3><div class="muted">${o.campaign}</div><div class="caption" style="margin-top:8px">${o.lastAction} · ${o.received}</div></div><div style="text-align:right"><div class="caption">Offer</div><div class="kpi" style="font-size:27px">$${o.payment.toLocaleString()}</div><div class="row" style="justify-content:flex-end;margin-top:10px"><button class="btn btn-success btn-sm" data-action="accept" data-id="${o.id}">Accept</button><button class="btn btn-outline btn-sm" data-action="revise" data-id="${o.id}">Revise</button><button class="btn btn-danger btn-sm" data-action="reject" data-id="${o.id}">Reject</button></div></div></div>`).join('');box.addEventListener('click',e=>{const b=e.target.closest('[data-action]');if(!b)return;CDOS.toast(`${b.dataset.action[0].toUpperCase()+b.dataset.action.slice(1)} action saved (demo)`)});
  });
})();
