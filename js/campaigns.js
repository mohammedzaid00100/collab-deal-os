(function(){document.addEventListener('DOMContentLoaded',async()=>{if(document.body.dataset.page!=='campaigns')return;const d=await CDOS.loadData();const local=CDOS.storage.get('campaigns',[]);const box=document.getElementById('campaignList');if(box)box.innerHTML=[...local,...d.campaigns].map(c=>`<div class="card dash-card row-between"><div><span class="pill ${c.status==='Active'?'green':''}">${c.status||'Draft'}</span><h3 style="margin:10px 0 4px">${c.name||c.campaignName||'Untitled Campaign'}</h3><div class="muted">${c.objective||'Creator collaboration campaign'}</div></div><div class="row"><a class="btn btn-outline btn-sm" href="create-campaign.html">Edit</a><a class="btn btn-primary btn-sm" href="creators.html">Find Creators</a></div></div>`).join('')})})();
