(function(){
  document.addEventListener('DOMContentLoaded',async()=>{if(document.body.dataset.page!=='ai')return;const d=await CDOS.loadData();const params=new URLSearchParams(location.search);const id=params.get('id');const o=d.opportunities.find(x=>x.id===id)||d.opportunities[0];const f=document.getElementById('aiForm');if(f){f.elements.payment.value=o?.payment||2000;f.elements.productValue.value=o?.productValue||180;f.elements.followers.value=d.creator.metrics.instagram;f.elements.averageViews.value=d.creator.metrics.averageViews;f.elements.engagement.value=d.creator.metrics.engagement;f.elements.niche.value=d.creator.niche;f.addEventListener('submit',e=>{e.preventDefault();analyze(Object.fromEntries(new FormData(f).entries()))});}
    if(document.getElementById('analysisResult')) analyze({payment:o?.payment||2000,productValue:o?.productValue||180,followers:d.creator.metrics.instagram,averageViews:d.creator.metrics.averageViews,engagement:d.creator.metrics.engagement,niche:d.creator.niche,deliverables:o?.deliverables?.length||2,usageMonths:3,exclusivity:'no'});
  });
  async function analyze(input){const local=PricingEngine.calculate(input);let result=local;try{const r=await fetch('../api/ai-analysis',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(input)});if(r.ok)result=await r.json()}catch{}render(result,input)}
  function render(r,input){const box=document.getElementById('analysisResult');if(!box)return;const curr=Number(input.payment||0);box.innerHTML=`<div class="ai-result"><div class="card result-card"><div class="caption">Current Offer</div><div class="value" style="color:var(--primary)">$${curr.toLocaleString()}</div><div class="caption">Proposed payment</div></div><div class="card result-card"><div class="caption">Fair Value Range</div><div class="value">$${r.low.toLocaleString()} – $${r.high.toLocaleString()}</div><div class="caption">Deterministic pricing engine · ${r.confidence}% confidence</div></div><div class="card result-card"><div class="caption">Deal Score</div><div class="value">${r.score}<span style="font-size:14px;color:var(--muted)">/100</span></div><div class="pill ${r.score>=70?'green':'red'}">${r.score>=70?'Strong':'Below Average'}</div></div></div><div class="recommend-banner"><div class="recommend-icon">✦</div><div><div class="caption">Our Recommendation</div><h2 style="margin:3px 0;color:var(--primary)">${r.score>=75?'Accept / Light Revision':'Counter Offer Recommended'}</h2><div class="muted">${r.explanation||`The offer is ${curr<r.low?'below':'within'} the modeled fair range. Suggested counter: $${r.counter.toLocaleString()}.`}</div></div></div><div class="card comparison" style="margin-top:16px"><h3>Offer Comparison</h3><div class="compare-line"><strong>Your Offer · $${curr.toLocaleString()}</strong><div class="compare-track"><span style="--left:0%;--width:${Math.min(100,curr/(r.high*1.25)*100)}%;--bar:#7658ff"></span></div></div><div class="compare-line"><strong>Fair Value Range</strong><div class="compare-track"><span style="--left:${r.low/(r.high*1.25)*100}%;--width:${(r.high-r.low)/(r.high*1.25)*100}%;--bar:#55c98b"></span></div></div><div class="row" style="margin-top:22px"><button class="btn btn-outline" data-demo-toast="Offer revision opened">Revise Offer</button><button class="btn btn-primary" data-demo-toast="Counter offer prepared">Counter Offer $${r.counter.toLocaleString()}</button><button class="btn btn-success" data-demo-toast="Offer accepted">Accept Offer</button></div></div>`;box.querySelectorAll('[data-demo-toast]').forEach(b=>b.addEventListener('click',()=>CDOS.toast(b.dataset.demoToast)))}
})();
